export const KEY_REGISTRY_ADD_EVENT_SIGNATURE = 'event Add( uint256 indexed fid, uint32 indexed keyType, bytes indexed key, bytes keyBytes, uint8 metadataType, bytes metadata )';

export const MESSAGE_VERSION = 0;